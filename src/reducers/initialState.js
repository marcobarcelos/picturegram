import { GRID_MODE_SMALL } from '../constants/GridModes';

export default {
  shots: {
    gridMode: GRID_MODE_SMALL,
    loading: false,
    error: null,
    items: [],
    selectedItem: null,
    links: {
      next: ''
    }
  }
};
